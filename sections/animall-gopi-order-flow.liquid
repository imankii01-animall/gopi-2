{{ 'animall-gopi.css' | asset_url | stylesheet_tag }}
<script src="{{ 'animall-gopi.js' | asset_url }}" defer="defer"></script>

{%- assign current_variant = product.selected_or_first_available_variant -%}
{%- if current_variant == blank and product.variants.size > 0 -%}
  {%- assign current_variant = product.variants.first -%}
{%- endif -%}
{%- assign tag_location = '' -%}
{%- assign tag_type = '' -%}
{%- assign tag_method = '' -%}

{%- for tag in product.tags -%}
  {%- assign normalized_tag = tag | downcase -%}

  {%- if tag_location == '' and normalized_tag contains 'location:' -%}
    {%- assign tag_location = tag | split: ':' | last | strip | replace: '_', ' ' | replace: '-', ' ' -%}
  {%- elsif tag_location == '' and normalized_tag contains 'city:' -%}
    {%- assign tag_location = tag | split: ':' | last | strip | replace: '_', ' ' | replace: '-', ' ' -%}
  {%- endif -%}

  {%- if tag_type == '' and normalized_tag contains 'type:' -%}
    {%- assign tag_type = tag | split: ':' | last | strip | replace: '_', ' ' | replace: '-', ' ' -%}
  {%- elsif tag_type == '' and normalized_tag contains 'ghee:' -%}
    {%- assign tag_type = tag | split: ':' | last | strip | replace: '_', ' ' | replace: '-', ' ' -%}
  {%- endif -%}

  {%- if tag_method == '' and normalized_tag contains 'method:' -%}
    {%- assign tag_method = tag | split: ':' | last | strip | replace: '_', ' ' | replace: '-', ' ' -%}
  {%- endif -%}
{%- endfor -%}

{%- assign farmer_name = product.metafields.custom.farmer_name.value | default: product.vendor | default: product.title -%}
{%- assign farmer_location = product.metafields.custom.farmer_location.value | default: product.metafields.custom.location.value | default: tag_location | default: section.settings.default_location -%}
{%- assign ghee_type = product.metafields.custom.ghee_type.value | default: tag_type | default: product.type | default: section.settings.default_ghee_type -%}
{%- assign production_method = product.metafields.custom.production_method.value | default: tag_method | default: section.settings.production_method_badge -%}
{%- assign rating = product.metafields.custom.rating.value | default: section.settings.default_rating -%}
{%- assign max_stock = current_variant.inventory_quantity | default: 0 -%}

{%- assign premium_explanation = product.metafields.custom.premium_explanation.value | default: section.settings.default_premium_explanation -%}
{%- assign production_steps_source = product.metafields.custom.production_steps.value | default: product.metafields.custom.production_process.value | default: section.settings.default_production_steps -%}
{%- assign production_steps = production_steps_source | newline_to_br | split: '<br />' -%}
{%- assign usage_guidelines_source = product.metafields.custom.platform_guidelines.value | default: product.metafields.custom.usage_guidelines.value | default: section.settings.default_usage_guidelines -%}
{%- assign usage_guidelines = usage_guidelines_source | newline_to_br | split: '<br />' -%}
{%- assign farm_gallery = product.metafields.custom.farm_gallery.value -%}

{%- assign whatsapp_number = product.metafields.custom.whatsapp_number.value | default: section.settings.whatsapp_number -%}
{%- assign whatsapp_prefill = product.metafields.custom.whatsapp_prefill.value | default: section.settings.whatsapp_prefill -%}
{%- assign whatsapp_digits = whatsapp_number | remove: ' ' | remove: '-' | remove: '(' | remove: ')' | remove: '+' -%}
{%- assign whatsapp_link = '' -%}
{%- if whatsapp_digits != blank -%}
  {%- capture whatsapp_message -%}{{ whatsapp_prefill }} {{ product.title }}{%- endcapture -%}
  {%- assign whatsapp_encoded_text = whatsapp_message | strip | url_encode -%}
  {%- assign whatsapp_link = 'https://wa.me/' | append: whatsapp_digits | append: '?text=' | append: whatsapp_encoded_text -%}
{%- endif -%}

{%- assign commission_percent = section.settings.commission_percent | default: 10 -%}
{%- assign initial_total_cents = current_variant.price | default: 0 -%}
{%- assign initial_commission_cents = initial_total_cents | times: commission_percent | divided_by: 100 -%}
{%- assign initial_net_cents = initial_total_cents | minus: initial_commission_cents -%}

<section
  class="ag-order-page ag-reveal ag-reveal--entry"
  id="AnimallOrderFlow-{{ section.id }}"
  data-ag-order-flow
  data-product-handle="{{ product.handle }}"
  data-unit-price="{{ current_variant.price }}"
  data-max-stock="{{ max_stock }}"
  data-commission-rate="{{ commission_percent }}"
  data-require-phone-verification="false"
  data-stock-flexible-label="{{ section.settings.stock_flexible_label | escape }}"
  data-verify-note-not-required="{{ section.settings.verify_not_required_label | escape }}"
  data-verify-note-stock-exceeded="{{ section.settings.stock_exceeded_label | escape }}"
  data-verify-note-order-failed="{{ section.settings.order_failed_label | escape }}"
  data-verify-note-stock-refresh-failed="{{ section.settings.stock_refresh_failed_label | escape }}"
  data-verify-note-sold-out="{{ section.settings.sold_out_note_label | escape }}"
  data-submit-ready-label="{{ section.settings.submit_ready_label | escape }}"
  data-submit-loading-label="{{ section.settings.submit_loading_label | escape }}"
  data-submit-sold-out-label="{{ section.settings.submit_sold_out_label | escape }}"
  data-unit-label="{{ section.settings.unit_label | escape }}"
  style="
    --ag-bg: {{ section.settings.bg_color }};
    --ag-surface: {{ section.settings.card_color }};
    --ag-surface-strong: {{ section.settings.control_bg_color }};
    --ag-border: {{ section.settings.border_color }};
    --ag-border-strong: {{ section.settings.border_strong_color }};
    --ag-text: {{ section.settings.text_color }};
    --ag-text-soft: {{ section.settings.text_soft_color }};
    --ag-primary: {{ section.settings.button_color }};
    --ag-primary-hover: {{ section.settings.button_hover_color }};
    --ag-primary-text: {{ section.settings.button_text_color }};
    --ag-badge: {{ section.settings.badge_color }};
    --ag-badge-text: {{ section.settings.badge_text_color }};
    --ag-accent: {{ section.settings.accent_color }};
  "
>
  <div class="ag-shell">
    <header class="ag-order-header">
      <a href="javascript:history.back()" class="ag-back-link" aria-label="{{ section.settings.back_link_aria_label }}">←</a>
      <h1>{{ section.settings.order_heading }}</h1>
    </header>

    <div class="ag-order-product-card">
      <div class="ag-order-product-main">
        <div class="ag-card-media">
          {%- if product.featured_image -%}
            {{
              product.featured_image
              | image_url: width: 280, height: 280, crop: 'center'
              | image_tag:
                loading: 'lazy',
                widths: '100,140,180,220,280',
                sizes: '(max-width: 749px) 92px, 108px',
                alt: farmer_name,
                class: 'ag-card-image'
            }}
          {%- else -%}
            <div class="ag-card-image ag-card-image--placeholder">{{ farmer_name | slice: 0 }}</div>
          {%- endif -%}
        </div>

        <div class="ag-order-product-content">
          <h2>{{ farmer_name }}</h2>
          <p class="ag-card-location">{{ farmer_location }}</p>

          <div class="ag-card-badges">
            <span class="ag-chip ag-chip--primary">{{ ghee_type }}</span>
            <span class="ag-chip">{{ production_method }}</span>
          </div>
        </div>
      </div>

      <div class="ag-order-product-side">
        <p class="ag-order-product-price" data-ag-summary-price>{{ current_variant.price | money_without_trailing_zeros }}</p>
        <p class="ag-order-product-unit">/{{ section.settings.unit_label }}</p>
        <p class="ag-card-rating" aria-label="Rating {{ rating }}">★ {{ rating }}</p>
      </div>
    </div>

    {%- if section.settings.show_story_sections -%}
      <section class="ag-panel ag-info-panel">
        <h3>{{ section.settings.premium_heading }}</h3>
        <p class="ag-panel-copy">{{ premium_explanation | newline_to_br }}</p>
      </section>

      <section class="ag-panel ag-process-panel">
        <h3>{{ section.settings.production_heading }}</h3>
        {%- assign has_production_steps = false -%}
        <ol class="ag-step-list">
          {%- for raw_step in production_steps -%}
            {%- assign step = raw_step | strip -%}
            {%- if step != blank -%}
              {%- assign has_production_steps = true -%}
              <li>{{ step }}</li>
            {%- endif -%}
          {%- endfor -%}
        </ol>
        {%- unless has_production_steps -%}
          <p class="ag-section-empty">{{ section.settings.no_production_steps_label }}</p>
        {%- endunless -%}
      </section>

      <section class="ag-panel ag-gallery-panel">
        <h3>{{ section.settings.gallery_heading }}</h3>
        {%- assign rendered_gallery = false -%}
        <div class="ag-gallery-grid">
          {%- if farm_gallery != blank -%}
            {%- for gallery_item in farm_gallery limit: 6 -%}
              {%- assign gallery_image = gallery_item.preview_image | default: gallery_item -%}
              {%- if gallery_image != blank -%}
                {%- assign rendered_gallery = true -%}
                <div class="ag-gallery-item">
                  {{
                    gallery_image
                    | image_url: width: 560, height: 560, crop: 'center'
                    | image_tag:
                      loading: 'lazy',
                      widths: '160,220,320,420,560',
                      sizes: '(max-width: 749px) 46vw, 180px',
                      alt: gallery_item.alt | default: farmer_name,
                      class: 'ag-gallery-image'
                  }}
                </div>
              {%- endif -%}
            {%- endfor -%}
          {%- endif -%}

          {%- unless rendered_gallery -%}
            {%- for image in product.images offset: 1 limit: 6 -%}
              {%- assign rendered_gallery = true -%}
              <div class="ag-gallery-item">
                {{
                  image
                  | image_url: width: 560, height: 560, crop: 'center'
                  | image_tag:
                    loading: 'lazy',
                    widths: '160,220,320,420,560',
                    sizes: '(max-width: 749px) 46vw, 180px',
                    alt: image.alt | default: farmer_name,
                    class: 'ag-gallery-image'
                }}
              </div>
            {%- endfor -%}
          {%- endunless -%}
        </div>
        {%- unless rendered_gallery -%}
          <p class="ag-section-empty">{{ section.settings.gallery_empty_label }}</p>
        {%- endunless -%}
      </section>

      <section class="ag-panel ag-guidelines-panel">
        <h3>{{ section.settings.guidelines_heading }}</h3>
        {%- assign has_guidelines = false -%}
        <ul class="ag-guidelines-list">
          {%- for raw_guideline in usage_guidelines -%}
            {%- assign guideline = raw_guideline | strip -%}
            {%- if guideline != blank -%}
              {%- assign has_guidelines = true -%}
              <li>{{ guideline }}</li>
            {%- endif -%}
          {%- endfor -%}
        </ul>
        {%- unless has_guidelines -%}
          <p class="ag-section-empty">{{ section.settings.no_guidelines_label }}</p>
        {%- endunless -%}
      </section>

      <section class="ag-panel ag-whatsapp-panel">
        <h3>{{ section.settings.whatsapp_heading }}</h3>
        <p class="ag-panel-copy">{{ section.settings.whatsapp_description }}</p>
        {%- if whatsapp_link != blank -%}
          <a class="ag-whatsapp-btn" href="{{ whatsapp_link }}" target="_blank" rel="noopener">
            {{ section.settings.whatsapp_cta_label }}
          </a>
        {%- else -%}
          <p class="ag-section-empty">{{ section.settings.whatsapp_missing_label }}</p>
        {%- endif -%}
      </section>
    {%- endif -%}

    <form method="post" action="/cart/add" enctype="multipart/form-data" class="ag-order-form" data-ag-order-form>
      <input type="hidden" name="id" value="{{ current_variant.id }}" data-ag-variant-id>
      <input type="hidden" name="quantity" value="1" data-ag-quantity-input>
      <input type="hidden" name="properties[Phone Verified]" value="Not Required" data-ag-phone-verified-field>
      <input type="hidden" name="properties[Source]" value="Animall Gopi MVP">
      <input type="hidden" name="properties[Farmer Name]" value="{{ farmer_name }}">
      <input type="hidden" name="properties[Farmer Location]" value="{{ farmer_location }}">
      <input type="hidden" name="properties[Ghee Type]" value="{{ ghee_type }}">
      <input type="hidden" name="properties[Order Total]" value="{{ initial_total_cents | money_without_trailing_zeros }}" data-ag-order-total-property>
      <input type="hidden" name="properties[Platform Commission]" value="{{ initial_commission_cents | money_without_trailing_zeros }}" data-ag-commission-property>
      <input type="hidden" name="properties[Net Payable to Farmer]" value="{{ initial_net_cents | money_without_trailing_zeros }}" data-ag-net-payable-property>
      <input type="hidden" name="properties[Commission Rate]" value="{{ commission_percent }}%" data-ag-commission-rate-property>

      <section class="ag-panel">
        <div class="ag-panel-heading">
          <h3>{{ section.settings.quantity_heading }}</h3>
          <p>{{ section.settings.max_stock_label_prefix }} <span data-ag-max-stock>{{ max_stock }}</span> {{ section.settings.stock_suffix_label }}</p>
        </div>

        <div class="ag-stepper" role="group" aria-label="Quantity selector">
          <button class="ag-stepper-btn" type="button" data-ag-minus aria-label="{{ section.settings.decrease_qty_aria_label }}">−</button>
          <div class="ag-stepper-value">
            <strong data-ag-quantity-display>1</strong>
            <span data-ag-unit-label>{{ section.settings.unit_label }}</span>
          </div>
          <button class="ag-stepper-btn" type="button" data-ag-plus aria-label="{{ section.settings.increase_qty_aria_label }}">+</button>
        </div>
      </section>

      <section class="ag-panel">
        <h3>{{ section.settings.customer_details_heading }}</h3>

        <label class="ag-field">
          <span>{{ section.settings.name_label }}</span>
          <input type="text" name="properties[Customer Name]" autocomplete="name" placeholder="{{ section.settings.name_placeholder }}" required>
        </label>

        <label class="ag-field">
          <span>{{ section.settings.phone_label }}</span>
          <div class="ag-phone-row">
            <input
              type="tel"
              name="properties[Phone Number]"
              inputmode="numeric"
              pattern="[0-9]{10}"
              maxlength="10"
              placeholder="{{ section.settings.phone_placeholder }}"
              autocomplete="tel"
              required
              data-ag-phone-input
            >
          </div>
        </label>

        <p class="ag-verify-note" data-ag-verify-note>
          {{ section.settings.verify_not_required_label }}
        </p>

        <label class="ag-field">
          <span>{{ section.settings.address_1_label }}</span>
          <input type="text" name="properties[Address Line 1]" autocomplete="address-line1" placeholder="{{ section.settings.address_1_placeholder }}" required>
        </label>

        <label class="ag-field">
          <span>{{ section.settings.address_2_label }}</span>
          <input type="text" name="properties[Address Line 2]" autocomplete="address-line2" placeholder="{{ section.settings.address_2_placeholder }}">
        </label>

        <div class="ag-grid ag-grid--two">
          <label class="ag-field">
            <span>{{ section.settings.city_label }}</span>
            <input type="text" name="properties[City]" autocomplete="address-level2" required>
          </label>
          <label class="ag-field">
            <span>{{ section.settings.state_label }}</span>
            <input type="text" name="properties[State]" autocomplete="address-level1" required>
          </label>
        </div>

        <label class="ag-field">
          <span>{{ section.settings.pin_label }}</span>
          <input type="text" name="properties[PIN]" inputmode="numeric" pattern="[0-9]{6}" maxlength="6" placeholder="{{ section.settings.pin_placeholder }}" required>
        </label>
      </section>

      <section class="ag-panel ag-summary-panel">
        <h3>{{ section.settings.order_summary_heading }}</h3>

        <dl class="ag-summary-list">
          <div>
            <dt>{{ section.settings.price_per_kg_label }}</dt>
            <dd data-ag-summary-price>{{ current_variant.price | money_without_trailing_zeros }}</dd>
          </div>
          <div>
            <dt>{{ section.settings.quantity_label }}</dt>
            <dd><span data-ag-summary-qty>1</span> <span data-ag-unit-label>{{ section.settings.unit_label }}</span></dd>
          </div>
          <div>
            <dt>
              {{ section.settings.commission_label }}
              <span class="ag-commission-rate" data-ag-commission-rate-label>({{ commission_percent }}%)</span>
            </dt>
            <dd data-ag-summary-commission>{{ initial_commission_cents | money_without_trailing_zeros }}</dd>
          </div>
          <div>
            <dt>{{ section.settings.net_payable_label }}</dt>
            <dd data-ag-summary-net-payable>{{ initial_net_cents | money_without_trailing_zeros }}</dd>
          </div>
          <div class="ag-summary-total">
            <dt>{{ section.settings.total_label }}</dt>
            <dd data-ag-summary-total>{{ current_variant.price | money_without_trailing_zeros }}</dd>
          </div>
        </dl>

        <p class="ag-summary-note">{{ section.settings.delivery_note }}</p>
      </section>

      <button class="ag-submit-btn" type="submit" data-ag-submit-btn disabled>
        {{ section.settings.verify_button_text }}
      </button>
    </form>
  </div>

  <script type="application/json" data-ag-variants-json>
    {{ product.variants | json }}
  </script>

  <nav class="ag-bottom-nav" aria-label="Mobile navigation">
    <a class="ag-bottom-nav-link" href="{{ section.settings.home_link | default: routes.root_url }}">
      <span aria-hidden="true">⌂</span>
      <span>{{ section.settings.home_label }}</span>
    </a>
    <a class="ag-bottom-nav-link is-active" href="{{ section.settings.orders_link | default: routes.account_url }}">
      <span aria-hidden="true">☐</span>
      <span>{{ section.settings.orders_label }}</span>
    </a>
    {%- if section.settings.emergent_badge_label != blank -%}
      {%- assign badge_link = section.settings.emergent_badge_link | default: routes.root_url -%}
      <a class="ag-bottom-nav-badge" href="{{ badge_link }}">
        {{ section.settings.emergent_badge_label }}
      </a>
    {%- endif -%}
  </nav>
</section>

{% schema %}
{
  "name": "Animall Gopi Order Flow",
  "templates": ["product"],
  "settings": [
    {
      "type": "text",
      "id": "order_heading",
      "label": "Page heading",
      "default": "Place Order / ऑर्डर करें"
    },
    {
      "type": "text",
      "id": "delivery_note",
      "label": "Delivery note",
      "default": "After payment, delivery will be managed via Shiprocket"
    },
    {
      "type": "text",
      "id": "verify_button_text",
      "label": "Primary button default label",
      "default": "Place Order & Pay"
    },
    {
      "type": "text",
      "id": "back_link_aria_label",
      "label": "Back button accessibility label",
      "default": "Go back"
    },
    {
      "type": "text",
      "id": "decrease_qty_aria_label",
      "label": "Decrease quantity accessibility label",
      "default": "Decrease quantity"
    },
    {
      "type": "text",
      "id": "increase_qty_aria_label",
      "label": "Increase quantity accessibility label",
      "default": "Increase quantity"
    },
    {
      "type": "header",
      "content": "Product Detail Content"
    },
    {
      "type": "checkbox",
      "id": "show_story_sections",
      "label": "Show premium/process/gallery/guidelines/whatsapp sections",
      "default": false
    },
    {
      "type": "text",
      "id": "premium_heading",
      "label": "Premium section heading",
      "default": "Why this ghee is premium"
    },
    {
      "type": "textarea",
      "id": "default_premium_explanation",
      "label": "Default premium explanation",
      "default": "Traditional bilona churning, small-batch preparation, and transparent sourcing directly from the farmer."
    },
    {
      "type": "text",
      "id": "production_heading",
      "label": "Production process heading",
      "default": "Production process"
    },
    {
      "type": "textarea",
      "id": "default_production_steps",
      "label": "Default production steps (one per line)",
      "default": "Milk collection from farm\nCurd fermentation for 10-12 hours\nTraditional bilona churning\nSlow heat clarification and filtering"
    },
    {
      "type": "text",
      "id": "no_production_steps_label",
      "label": "No production steps label",
      "default": "Production process details will be updated soon."
    },
    {
      "type": "text",
      "id": "gallery_heading",
      "label": "Farm gallery heading",
      "default": "Farm gallery"
    },
    {
      "type": "text",
      "id": "gallery_empty_label",
      "label": "Farm gallery empty label",
      "default": "Farm gallery images are not available yet."
    },
    {
      "type": "text",
      "id": "guidelines_heading",
      "label": "Usage guidelines heading",
      "default": "Platform usage guidelines"
    },
    {
      "type": "textarea",
      "id": "default_usage_guidelines",
      "label": "Default usage guidelines (one per line)",
      "default": "Use this channel only for product quality and sourcing queries.\nDelivery ETA is confirmed after payment.\nOrders are processed by Animall operations team."
    },
    {
      "type": "text",
      "id": "no_guidelines_label",
      "label": "No guidelines label",
      "default": "Usage guidelines are not configured yet."
    },
    {
      "type": "text",
      "id": "whatsapp_heading",
      "label": "WhatsApp section heading",
      "default": "Quality queries on WhatsApp"
    },
    {
      "type": "textarea",
      "id": "whatsapp_description",
      "label": "WhatsApp description",
      "default": "For transparency and quality-related questions, contact Animall caller on WhatsApp."
    },
    {
      "type": "text",
      "id": "whatsapp_cta_label",
      "label": "WhatsApp button label",
      "default": "Chat on WhatsApp"
    },
    {
      "type": "text",
      "id": "whatsapp_missing_label",
      "label": "WhatsApp missing label",
      "default": "WhatsApp contact is not configured."
    },
    {
      "type": "text",
      "id": "whatsapp_number",
      "label": "Default WhatsApp number (country code + number)",
      "default": "919876543210"
    },
    {
      "type": "text",
      "id": "whatsapp_prefill",
      "label": "WhatsApp prefilled message prefix",
      "default": "Hi Animall team, I have a quality query for"
    },
    {
      "type": "header",
      "content": "Order and Commission"
    },
    {
      "type": "text",
      "id": "quantity_heading",
      "label": "Quantity section heading",
      "default": "मात्रा (Quantity)"
    },
    {
      "type": "text",
      "id": "max_stock_label_prefix",
      "label": "Max stock prefix",
      "default": "Max:"
    },
    {
      "type": "text",
      "id": "stock_suffix_label",
      "label": "Stock suffix label",
      "default": "kg available"
    },
    {
      "type": "text",
      "id": "unit_label",
      "label": "Unit label",
      "default": "kg"
    },
    {
      "type": "range",
      "id": "commission_percent",
      "label": "Platform commission (%)",
      "min": 0,
      "max": 30,
      "step": 1,
      "default": 10
    },
    {
      "type": "text",
      "id": "customer_details_heading",
      "label": "Customer details heading",
      "default": "आपकी जानकारी (Your Details)"
    },
    {
      "type": "text",
      "id": "order_summary_heading",
      "label": "Order summary heading",
      "default": "ऑर्डर सारांश (Order Summary)"
    },
    {
      "type": "text",
      "id": "price_per_kg_label",
      "label": "Price per kg label",
      "default": "Price per kg"
    },
    {
      "type": "text",
      "id": "quantity_label",
      "label": "Quantity label",
      "default": "Quantity"
    },
    {
      "type": "text",
      "id": "commission_label",
      "label": "Commission line label",
      "default": "Platform commission"
    },
    {
      "type": "text",
      "id": "net_payable_label",
      "label": "Net payable line label",
      "default": "Net payable to farmer"
    },
    {
      "type": "text",
      "id": "total_label",
      "label": "Total label",
      "default": "Total"
    },
    {
      "type": "header",
      "content": "Customer Form Labels"
    },
    {
      "type": "text",
      "id": "name_label",
      "label": "Name field label",
      "default": "Your Name / आपका नाम"
    },
    {
      "type": "text",
      "id": "name_placeholder",
      "label": "Name placeholder",
      "default": "Enter your name"
    },
    {
      "type": "text",
      "id": "phone_label",
      "label": "Phone field label",
      "default": "Phone Number / फोन नंबर"
    },
    {
      "type": "text",
      "id": "phone_placeholder",
      "label": "Phone placeholder",
      "default": "10-digit phone number"
    },
    {
      "type": "text",
      "id": "address_1_label",
      "label": "Address line 1 label",
      "default": "Address Line 1"
    },
    {
      "type": "text",
      "id": "address_1_placeholder",
      "label": "Address line 1 placeholder",
      "default": "House / Street"
    },
    {
      "type": "text",
      "id": "address_2_label",
      "label": "Address line 2 label",
      "default": "Address Line 2 (Optional)"
    },
    {
      "type": "text",
      "id": "address_2_placeholder",
      "label": "Address line 2 placeholder",
      "default": "Area / Landmark"
    },
    {
      "type": "text",
      "id": "city_label",
      "label": "City label",
      "default": "City"
    },
    {
      "type": "text",
      "id": "state_label",
      "label": "State label",
      "default": "State"
    },
    {
      "type": "text",
      "id": "pin_label",
      "label": "PIN label",
      "default": "PIN Code"
    },
    {
      "type": "text",
      "id": "pin_placeholder",
      "label": "PIN placeholder",
      "default": "6-digit PIN"
    },
    {
      "type": "text",
      "id": "verify_not_required_label",
      "label": "Phone message",
      "default": "Place order directly after filling details."
    },
    {
      "type": "text",
      "id": "stock_exceeded_label",
      "label": "Stock exceeded message",
      "default": "Quantity exceeds available stock."
    },
    {
      "type": "text",
      "id": "stock_refresh_failed_label",
      "label": "Stock refresh failed message",
      "default": "Could not refresh live stock. Proceeding with current stock value."
    },
    {
      "type": "text",
      "id": "order_failed_label",
      "label": "Order failed message",
      "default": "Could not place the order. Please try again."
    },
    {
      "type": "text",
      "id": "sold_out_note_label",
      "label": "Sold out note message",
      "default": "This listing is currently out of stock."
    },
    {
      "type": "text",
      "id": "submit_ready_label",
      "label": "Submit ready label",
      "default": "Place Order & Pay"
    },
    {
      "type": "text",
      "id": "submit_loading_label",
      "label": "Submit loading label",
      "default": "Adding to cart..."
    },
    {
      "type": "text",
      "id": "submit_sold_out_label",
      "label": "Submit sold out label",
      "default": "Sold Out"
    },
    {
      "type": "text",
      "id": "stock_flexible_label",
      "label": "Stock label for untracked inventory",
      "default": "25+"
    },
    {
      "type": "header",
      "content": "Fallback Product Data"
    },
    {
      "type": "text",
      "id": "default_location",
      "label": "Fallback location",
      "default": "Gujarat"
    },
    {
      "type": "text",
      "id": "default_ghee_type",
      "label": "Fallback ghee type",
      "default": "A2 Cow Ghee"
    },
    {
      "type": "text",
      "id": "production_method_badge",
      "label": "Production method badge",
      "default": "Bilona"
    },
    {
      "type": "text",
      "id": "default_rating",
      "label": "Fallback rating",
      "default": "4.8"
    },
    {
      "type": "header",
      "content": "Theme Colors"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background color",
      "default": "#f5f3eb"
    },
    {
      "type": "color",
      "id": "card_color",
      "label": "Card color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "control_bg_color",
      "label": "Input background color",
      "default": "#f5f2e6"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "Border color",
      "default": "#e4decc"
    },
    {
      "type": "color",
      "id": "border_strong_color",
      "label": "Strong border color",
      "default": "#d5cda9"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Primary text color",
      "default": "#23211b"
    },
    {
      "type": "color",
      "id": "text_soft_color",
      "label": "Secondary text color",
      "default": "#666153"
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Button color",
      "default": "#202127"
    },
    {
      "type": "color",
      "id": "button_hover_color",
      "label": "Button hover color",
      "default": "#2d2f36"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button text color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "badge_color",
      "label": "Badge background color",
      "default": "#f1ebbe"
    },
    {
      "type": "color",
      "id": "badge_text_color",
      "label": "Badge text color",
      "default": "#4f4728"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent color",
      "default": "#d89f33"
    },
    {
      "type": "url",
      "id": "home_link",
      "label": "Bottom nav Home link"
    },
    {
      "type": "text",
      "id": "home_label",
      "label": "Bottom nav Home text",
      "default": "Home"
    },
    {
      "type": "url",
      "id": "orders_link",
      "label": "Bottom nav Orders link"
    },
    {
      "type": "text",
      "id": "orders_label",
      "label": "Bottom nav Orders text",
      "default": "Orders"
    },
    {
      "type": "url",
      "id": "profile_link",
      "label": "Bottom nav Profile link"
    },
    {
      "type": "text",
      "id": "profile_label",
      "label": "Bottom nav Profile text",
      "default": "Profile"
    },
    {
      "type": "url",
      "id": "emergent_badge_link",
      "label": "Bottom badge link"
    },
    {
      "type": "text",
      "id": "emergent_badge_label",
      "label": "Bottom badge text",
      "default": "Made with Emergent"
    }
  ],
  "presets": [
    {
      "name": "Animall Gopi Order Flow"
    }
  ]
}
{% endschema %}
